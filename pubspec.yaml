name: signals_watch
description: A production-ready reactive widget for signals_flutter with lifecycle callbacks, debouncing, throttling, error handling, async helpers, and registry management.
version: 2.0.0
homepage: https://github.com/soundsliced/signals_watch
repository: https://github.com/soundsliced/signals_watch
issue_tracker: https://github.com/soundsliced/signals_watch/issues
topics:
  - flutter
  - s_packages
  - reactive
  - state-management
  - widget

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  s_packages: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
